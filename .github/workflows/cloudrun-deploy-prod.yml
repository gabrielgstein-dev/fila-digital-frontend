name: Deploy Frontend to Cloud Run (Production)

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'  # Apenas tags sem sufixo (ex: 1.2.3)
  workflow_dispatch:
    inputs:
      tag:
        description: 'Production tag to deploy (without -stage suffix)'
        required: true
        default: '1.0.0'

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID_PROD }}
  GCP_REGION: ${{ vars.GCP_REGION_PROD }}
  SERVICE_NAME: ${{ vars.FRONTEND_SERVICE_NAME_PROD }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production # Requer aprovação/permissão
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set default environment variables
      run: |
        if [ -z "$GCP_PROJECT_ID" ]; then
          echo "GCP_PROJECT_ID not set, using default"
          echo "GCP_PROJECT_ID=fila-digital-qa" >> $GITHUB_ENV
        fi
        if [ -z "$GCP_REGION" ]; then
          echo "GCP_REGION not set, using default"
          echo "GCP_REGION=us-central1" >> $GITHUB_ENV
        fi
        if [ -z "$SERVICE_NAME" ]; then
          echo "SERVICE_NAME not set, using default"
          echo "SERVICE_NAME=fila-frontend" >> $GITHUB_ENV
        fi
        echo "Using GCP_PROJECT_ID: $GCP_PROJECT_ID"
        echo "Using GCP_REGION: $GCP_REGION"
        echo "Using SERVICE_NAME: $SERVICE_NAME"
        
        # Forçar definição das variáveis
        export GCP_PROJECT_ID="${GCP_PROJECT_ID:-fila-digital-qa}"
        export GCP_REGION="${GCP_REGION:-us-central1}"
        export SERVICE_NAME="${SERVICE_NAME:-fila-frontend}"
        
        echo "Final GCP_PROJECT_ID: $GCP_PROJECT_ID"
        echo "Final GCP_REGION: $GCP_REGION"
        echo "Final SERVICE_NAME: $SERVICE_NAME"

    - name: Extract version from tag
      id: version_check
      run: |
        if [[ "${{ github.event_name }}" == "push" && "${{ github.ref_type }}" == "tag" ]]; then
          # Extract version from tag
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "Tag detectada: $TAG_NAME"
          
          # Validar que NÃO contém -stage e segue formato X.Y.Z
          if [[ "$TAG_NAME" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "should_deploy=true" >> $GITHUB_OUTPUT
            echo "version=$TAG_NAME" >> $GITHUB_OUTPUT
            echo "✅ Deploy de PRODUÇÃO será executado para versão: $TAG_NAME"
          else
            echo "should_deploy=false" >> $GITHUB_OUTPUT
            echo "❌ Tag não é válida para produção (deve ser X.Y.Z sem sufixo)"
          fi
        elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
          # Manual dispatch
          TAG_NAME="${{ github.event.inputs.tag }}"
          echo "should_deploy=true" >> $GITHUB_OUTPUT
          echo "version=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "✅ Deploy manual de PRODUÇÃO para versão: $TAG_NAME"
        else
          echo "should_deploy=false" >> $GITHUB_OUTPUT
          echo "❌ Evento não suportado para deploy de produção"
        fi

    - name: Setup Google Cloud CLI
      if: steps.version_check.outputs.should_deploy == 'true'
      uses: google-github-actions/setup-gcloud@v2
      with:
        service_account_key: ${{ secrets.GCP_SA_KEY_PROD }}
        project_id: ${{ secrets.GCP_PROJECT_ID_PROD }}

    - name: Configure project explicitly
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        echo "🔧 Configurando projeto explicitamente..."
        gcloud config set project $GCP_PROJECT_ID
        echo "✅ Projeto configurado: $(gcloud config get-value project)"

    - name: Debug environment variables
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        echo "🔍 Debug das variáveis de ambiente:"
        echo "GCP_PROJECT_ID: $GCP_PROJECT_ID"
        echo "GCP_REGION: $GCP_REGION"
        echo "SERVICE_NAME: $SERVICE_NAME"
        echo "GOOGLE_APPLICATION_CREDENTIALS: $GOOGLE_APPLICATION_CREDENTIALS"
        echo "CLOUDSDK_PROJECT: $CLOUDSDK_PROJECT"
        echo ""
        echo "🔍 Verificando autenticação:"
        gcloud auth list
        echo ""
        echo "🔍 Verificando projeto configurado:"
        gcloud config get-value project
        echo ""
        echo "🔍 Verificando repositórios disponíveis:"
        gcloud artifacts repositories list --location=us

    - name: Configure Docker to use gcloud as a credential helper
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        echo "🔧 Configurando Docker..."
        gcloud auth configure-docker us-docker.pkg.dev
        echo "✅ Docker configurado para us-docker.pkg.dev"
        
        # Verificar se o Docker está autenticado
        echo "🔍 Verificando autenticação do Docker..."
        docker login us-docker.pkg.dev

    - name: Build Docker image
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        VERSION="${{ steps.version_check.outputs.version }}"
        docker build -t us-docker.pkg.dev/$GCP_PROJECT_ID/fila-frontend/fila-frontend:$VERSION \
                     -t us-docker.pkg.dev/$GCP_PROJECT_ID/fila-frontend/fila-frontend:$GITHUB_SHA \
                     -t us-docker.pkg.dev/$GCP_PROJECT_ID/fila-frontend/fila-frontend:latest .

    - name: Push Docker image
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        VERSION="${{ steps.version_check.outputs.version }}"
        docker push us-docker.pkg.dev/$GCP_PROJECT_ID/fila-frontend/fila-frontend:$VERSION
        docker push us-docker.pkg.dev/$GCP_PROJECT_ID/fila-frontend/fila-frontend:$GITHUB_SHA
        docker push us-docker.pkg.dev/$GCP_PROJECT_ID/fila-frontend/fila-frontend:latest

    - name: Deploy to Cloud Run
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        gcloud run deploy $SERVICE_NAME \
          --image us-docker.pkg.dev/$GCP_PROJECT_ID/fila-frontend/fila-frontend:$GITHUB_SHA \
          --region $GCP_REGION \
          --platform managed \
          --allow-unauthenticated \
          --port 8080 \
          --memory 1Gi \
          --cpu 2 \
          --min-instances 1 \
          --max-instances 20 \
          --set-env-vars NODE_ENV=production,ENVIRONMENT=production,NEXT_TELEMETRY_DISABLED=${{ vars.NEXT_TELEMETRY_DISABLED }},NEXT_PUBLIC_API_URL=${{ vars.NEXT_PUBLIC_API_URL_PROD }},NEXT_PUBLIC_WS_URL=${{ vars.NEXT_PUBLIC_WS_URL_PROD }},NEXT_PUBLIC_APP_NAME="${{ vars.NEXT_PUBLIC_APP_NAME_PROD }}"

    - name: Get service URL
      if: steps.version_check.outputs.should_deploy == 'true'
      run: |
        SERVICE_URL=$(gcloud run services describe $SERVICE_NAME --region $GCP_REGION --format="value(status.url)")
        VERSION="${{ steps.version_check.outputs.version }}"
        echo "🚀 Frontend PRODUÇÃO deployed successfully!"
        echo "📦 Version: $VERSION"
        echo "🌐 URL: $SERVICE_URL"

    - name: Skip deployment message
      if: steps.version_check.outputs.should_deploy == 'false'
      run: |
        echo "⏭️ Deploy pulado - Tag não é válida para produção"
        echo "💡 Para deploy de produção, use:"
        echo "   pnpm run version:prod-patch  (cria tag X.X.X)"
        echo "   pnpm run version:prod-minor  (cria tag X.X.X)"
        echo "   pnpm run version:prod-major  (cria tag X.X.X)"
        echo ""
        echo "🏷️ Tags de produção devem seguir formato X.Y.Z (sem sufixo)"
        echo "🔒 Ambiente de produção requer permissões especiais"
